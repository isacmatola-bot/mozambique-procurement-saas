{
  "name": "mozambique-procurement-saas",
  "version": "1.0.0",
  "private": true,
  "description": "Launchable procurement, contracts, supplier selection and invoice SaaS for Instituto de Formação de Professores de Inhamizua.",
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w backend && npm run build -w frontend",
    "start": "npm run start -w backend",
    "db:migrate": "npm run db:migrate -w backend",
    "db:seed": "npm run db:seed -w backend",
    "test": "npm run test -w backend && npm run test -w frontend",
    "lint": "npm run lint -w backend && npm run lint -w frontend",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.3.3"
  },
  "engines": { "node": ">=20" },
  "keywords": ["mozambique", "procurement", "supabase", "postgresql", "contracts", "invoices", "openai"],
  "author": "Isac Matola",
  "license": "PROPRIETARY"
}
